# wasm-pack build output (regenerated by `npm run build:wasm`)
/pkg/

# Real FieldWorks project backups you drop in locally -- never public.
/fwbackups/*
!/fwbackups/.gitkeep

# Auto-extracted + converted grammar data derived from those backups -- also never public
# (derived data from a real linguistic project is still real project data).
/.data/

node_modules/
